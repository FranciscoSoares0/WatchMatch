import { ConfirmPasswordValidatorTs } from './confirm-password-validator.ts';

describe('ConfirmPasswordValidatorTs', () => {
  it('should create an instance', () => {
    expect(new ConfirmPasswordValidatorTs()).toBeTruthy();
  });
});
